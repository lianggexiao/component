<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qing.simpleframe.repository.DemoRepository">
    <resultMap id="User" type="com.qing.simpleframe.domain.entity.User">
        <result column="id" property="id" />
        <result column="name" property="name" />
        <result column="age" property="age" />
    </resultMap>
	<insert id="insertOne" parameterType="com.qing.simpleframe.domain.entity.User">
		insert into
		USER
		(
		id,
		name,
		age
		)
		values
		(#{id},#{name},#{age})
	</insert>
	
	<select id="findByName" resultMap="User" parameterType="java.lang.String">
        select id , name , age
        from user
        where name = #{name}
    </select>
</mapper>